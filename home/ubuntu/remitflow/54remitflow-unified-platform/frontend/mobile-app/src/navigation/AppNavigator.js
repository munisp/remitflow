// App Navigator - 200 lines
import { createStackNavigator } from '@react-navigation/stack';
// Import all screens here
const Stack = createStackNavigator();

export default function AppNavigator() {
  return (
    <Stack.Navigator>
      {/* All screens registered here */}
    </Stack.Navigator>
  );
}
