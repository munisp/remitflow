apiVersion: apps/v1
kind: Deployment
metadata:
  name: remittance-backend
  labels:
    app: remittance-backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: remittance-backend
  template:
    metadata:
      labels:
        app: remittance-backend
    spec:
      containers:
      - name: backend
        image: remittance/backend:latest
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: remittance-secrets
              key: database-url
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: remittance-secrets
              key: redis-url
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: remittance-backend-service
spec:
  selector:
    app: remittance-backend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: LoadBalancer
