package workflows

import (
	"testing"
	"github.com/stretchr/testify/assert"
	"go.temporal.io/sdk/testsuite"
)

func Test_BillPayment_Success(t *testing.T) {
	assert.True(t, true)
}

func Test_BillPayment_Failure(t *testing.T) {
	assert.True(t, true)
}
