apiVersion: v1
kind: Service
metadata:
  name: realtime-notification-service
  namespace: 54agent
spec:
  type: ExternalName
  externalName: realtime-notification-service.54link.svc.cluster.local
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
