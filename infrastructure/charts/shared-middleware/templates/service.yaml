apiVersion: v1
kind: Service
metadata:
  name: {{ include "shared-middleware.fullname" . }}
  labels:
    {{- include "shared-middleware.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "shared-middleware.selectorLabels" . | nindent 4 }}
