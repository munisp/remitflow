apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-security-sidecar.fullname" . }}
  labels:
    {{- include "go-security-sidecar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "go-security-sidecar.selectorLabels" . | nindent 4 }}
