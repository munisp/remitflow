apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-export-service.fullname" . }}
  labels:
    {{- include "go-export-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "go-export-service.selectorLabels" . | nindent 4 }}
