apiVersion: v1
kind: Service
metadata:
  name: {{ include "transfer-engine.fullname" . }}
  labels:
    {{- include "transfer-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "transfer-engine.selectorLabels" . | nindent 4 }}
