apiVersion: v1
kind: Service
metadata:
  name: {{ include "kyc-event-consumer.fullname" . }}
  labels:
    {{- include "kyc-event-consumer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "kyc-event-consumer.selectorLabels" . | nindent 4 }}
