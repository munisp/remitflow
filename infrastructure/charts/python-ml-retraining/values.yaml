replicaCount: 1
image:
  repository: registry.digitalocean.com/talentgraph-auth/54remit-python-ml-retraining
  pullPolicy: IfNotPresent
  tag: 0.1.0
nameOverride: ''
fullnameOverride: ''
serviceAccount:
  create: false
  name: 54remit
podAnnotations: {}
podLabels: {}
podSecurityContext: {}
securityContext: {}
service:
  type: ClusterIP
  port: 80
  targetPort: 8116
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 500Mi
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80
volumes: []
volumeMounts: []
nodeSelector: {}
tolerations: []
affinity: {}
dapr:
  appId: python-ml-retraining
  appPort: 8116
  enableMetrics: true
  enabled: true
  metricsPort: 9099
  sidecarListenAddresses: "0.0.0.0"
  cpu-request: 100m
  cpu-limit: 300m
  memory-request: 250Mi
  memory-limit: 1000Mi
# Auto-generated: env var names below were discovered via source grep
# (os.Getenv / env::var / os.getenv across python-ml-retraining's codebase), so this list
# reflects what the service actually reads — not a guess. Known non-secret
# infra defaults (Kafka, Dapr, log level, etc.) are pre-filled; everything
# else is empty and REQUIRED at deploy time (set via --set or a secrets manager), never committed:
#   helm upgrade --install python-ml-retraining infrastructure/charts/python-ml-retraining \
#     -n 54remit -f infrastructure/values-secrets/python-ml-retraining.yaml
# (a gitignored, per-environment file).
secrets:
  DATABASE_URL: ""  # REQUIRED
  DATA_DIR: ""  # REQUIRED
  DRIFT_CHECK_INTERVAL_HOURS: ""  # REQUIRED
  FX_FORECAST_SERVICE_URL: ""  # REQUIRED
  GNN_FRAUD_SERVICE_URL: ""  # REQUIRED
  INVESTMENT_ML_SERVICE_URL: ""  # REQUIRED
  MIN_NEW_SAMPLES_TO_RETRAIN: ""  # REQUIRED
  MLFLOW_REGISTRY_SERVICE_URL: ""  # REQUIRED
  NLU_SERVICE_URL: ""  # REQUIRED
  PORT: "8116"
  RAY_TRAINING_SERVICE_URL: ""  # REQUIRED
  RETRAIN_INTERVAL_HOURS: ""  # REQUIRED