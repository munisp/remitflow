apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-ml-retraining.fullname" . }}
  labels:
    {{- include "python-ml-retraining.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "python-ml-retraining.selectorLabels" . | nindent 4 }}
