apiVersion: v1
kind: Service
metadata:
  name: {{ include "rust-liveness-proxy.fullname" . }}
  labels:
    {{- include "rust-liveness-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "rust-liveness-proxy.selectorLabels" . | nindent 4 }}
