apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "caddy.fullname" . }}-data
  labels:
    {{- include "caddy.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
