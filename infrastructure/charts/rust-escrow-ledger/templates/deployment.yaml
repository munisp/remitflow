apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rust-escrow-ledger.fullname" . }}
  labels:
    {{- include "rust-escrow-ledger.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "rust-escrow-ledger.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        dapr.io/app-id: "{{ .Values.dapr.appId }}"
        dapr.io/app-port: "{{ .Values.dapr.appPort }}"
        dapr.io/enable-metrics: "{{ .Values.dapr.enableMetrics }}"
        dapr.io/enabled: "{{ .Values.dapr.enabled }}"
        dapr.io/metrics-port: "{{ .Values.dapr.metricsPort }}"
        dapr.io/sidecar-listen-addresses: "{{ .Values.dapr.sidecarListenAddresses }}"
        {{- if .Values.dapr.appProtocol }}
        dapr.io/app-protocol: "{{ .Values.dapr.appProtocol }}"
        {{- end }}
        dapr.io/log-level: "debug"
        dapr.io/sidecar-cpu-request:    '{{ index .Values.dapr "cpu-request" }}'
        dapr.io/sidecar-cpu-limit:      '{{ index .Values.dapr "cpu-limit" }}'
        dapr.io/sidecar-memory-request: '{{ index .Values.dapr "memory-request" }}'
        dapr.io/sidecar-memory-limit:   '{{ index .Values.dapr "memory-limit" }}'
      labels:
        {{- include "rust-escrow-ledger.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "rust-escrow-ledger.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          envFrom:
            - secretRef:
                name: {{ include "rust-escrow-ledger.fullname" . }}-secrets
          {{- with .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.volumeMounts }}
          volumeMounts:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.volumes }}
      volumes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
