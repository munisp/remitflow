apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-lakehouse-service.fullname" . }}
  labels:
    {{- include "python-lakehouse-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "python-lakehouse-service.selectorLabels" . | nindent 4 }}
