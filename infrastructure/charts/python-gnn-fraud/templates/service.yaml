apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-gnn-fraud.fullname" . }}
  labels:
    {{- include "python-gnn-fraud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "python-gnn-fraud.selectorLabels" . | nindent 4 }}
