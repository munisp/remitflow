apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-africbdc-adapter.fullname" . }}
  labels:
    {{- include "python-africbdc-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "python-africbdc-adapter.selectorLabels" . | nindent 4 }}
