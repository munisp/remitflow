replicaCount: 1
image:
  repository: registry.digitalocean.com/talentgraph-services/54remit-python-africbdc-adapter
  pullPolicy: IfNotPresent
  tag: 0.1.0
nameOverride: ''
fullnameOverride: ''
serviceAccount:
  create: false
  name: 54remit
podAnnotations: {}
podLabels: {}
podSecurityContext: {}
securityContext: {}
service:
  type: ClusterIP
  port: 8096
  targetPort: 8096
resources:
  requests:
    cpu: 100m
    memory: 250Mi
  limits:
    cpu: 500m
    memory: 1000Mi
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80
volumes: []
volumeMounts: []
nodeSelector: {}
tolerations: []
affinity: {}
dapr:
  appId: python-africbdc-adapter
  appPort: 8096
  enableMetrics: true
  enabled: true
  metricsPort: 9099
  sidecarListenAddresses: "0.0.0.0"
  cpu-request: 100m
  cpu-limit: 300m
  memory-request: 250Mi
  memory-limit: 1000Mi
# ⚠️ Do not commit real values here — see admin-service/values.yaml's comment
# for the override convention. This service holds the most sensitive
# credentials of the three (Keycloak admin password, superadmin bootstrap
# secret) — treat its override file with extra care (e.g. sealed-secrets or
# a secrets manager, not a plain gitignored yaml, once one is adopted).
secrets:
  CBDC_API_URL: ""  # REQUIRED — set via --set or secrets manager
  CBDC_API_KEY: ""  # REQUIRED — set via --set or secrets manager