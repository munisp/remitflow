apiVersion: v1
kind: Service
metadata:
  name: {{ include "rust-hnw-fx-engine.fullname" . }}
  labels:
    {{- include "rust-hnw-fx-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "rust-hnw-fx-engine.selectorLabels" . | nindent 4 }}
