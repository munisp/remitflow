apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-bricspay-adapter.fullname" . }}
  labels:
    {{- include "go-bricspay-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "go-bricspay-adapter.selectorLabels" . | nindent 4 }}
