apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-mlflow-registry.fullname" . }}
  labels:
    {{- include "python-mlflow-registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "python-mlflow-registry.selectorLabels" . | nindent 4 }}
