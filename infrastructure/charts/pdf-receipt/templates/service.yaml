apiVersion: v1
kind: Service
metadata:
  name: {{ include "pdf-receipt.fullname" . }}
  labels:
    {{- include "pdf-receipt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "pdf-receipt.selectorLabels" . | nindent 4 }}
