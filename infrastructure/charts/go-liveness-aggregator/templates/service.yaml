apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-liveness-aggregator.fullname" . }}
  labels:
    {{- include "go-liveness-aggregator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "go-liveness-aggregator.selectorLabels" . | nindent 4 }}
