apiVersion: v1
kind: Secret
metadata:
  name: {{ include "go-ghipss-adapter.fullname" . }}-secrets
type: Opaque
stringData:
{{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
