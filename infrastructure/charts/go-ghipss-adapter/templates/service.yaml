apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-ghipss-adapter.fullname" . }}
  labels:
    {{- include "go-ghipss-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "go-ghipss-adapter.selectorLabels" . | nindent 4 }}
