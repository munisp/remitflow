apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-pix-adapter.fullname" . }}
  labels:
    {{- include "python-pix-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "python-pix-adapter.selectorLabels" . | nindent 4 }}
