apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-synthetic-monitor.fullname" . }}
  labels:
    {{- include "python-synthetic-monitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "python-synthetic-monitor.selectorLabels" . | nindent 4 }}
