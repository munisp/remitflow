apiVersion: v1
kind: Service
metadata:
  name: {{ include "mojaloop-connector.fullname" . }}
  labels:
    {{- include "mojaloop-connector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "mojaloop-connector.selectorLabels" . | nindent 4 }}
