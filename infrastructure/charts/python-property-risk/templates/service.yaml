apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-property-risk.fullname" . }}
  labels:
    {{- include "python-property-risk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "python-property-risk.selectorLabels" . | nindent 4 }}
