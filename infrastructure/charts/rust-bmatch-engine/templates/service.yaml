apiVersion: v1
kind: Service
metadata:
  name: {{ include "rust-bmatch-engine.fullname" . }}
  labels:
    {{- include "rust-bmatch-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "rust-bmatch-engine.selectorLabels" . | nindent 4 }}
