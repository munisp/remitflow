apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-settlement-registry.fullname" . }}
  labels:
    {{- include "go-settlement-registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "go-settlement-registry.selectorLabels" . | nindent 4 }}
