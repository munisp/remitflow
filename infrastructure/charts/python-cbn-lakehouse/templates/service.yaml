apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-cbn-lakehouse.fullname" . }}
  labels:
    {{- include "python-cbn-lakehouse.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "python-cbn-lakehouse.selectorLabels" . | nindent 4 }}
