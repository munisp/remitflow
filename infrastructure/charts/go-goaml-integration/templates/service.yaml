apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-goaml-integration.fullname" . }}
  labels:
    {{- include "go-goaml-integration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "go-goaml-integration.selectorLabels" . | nindent 4 }}
