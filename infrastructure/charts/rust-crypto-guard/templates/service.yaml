apiVersion: v1
kind: Service
metadata:
  name: {{ include "rust-crypto-guard.fullname" . }}
  labels:
    {{- include "rust-crypto-guard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "rust-crypto-guard.selectorLabels" . | nindent 4 }}
