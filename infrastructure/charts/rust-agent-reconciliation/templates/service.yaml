apiVersion: v1
kind: Service
metadata:
  name: {{ include "rust-agent-reconciliation.fullname" . }}
  labels:
    {{- include "rust-agent-reconciliation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "rust-agent-reconciliation.selectorLabels" . | nindent 4 }}
