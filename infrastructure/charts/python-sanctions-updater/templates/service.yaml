apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-sanctions-updater.fullname" . }}
  labels:
    {{- include "python-sanctions-updater.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "python-sanctions-updater.selectorLabels" . | nindent 4 }}
