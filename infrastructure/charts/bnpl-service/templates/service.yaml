apiVersion: v1
kind: Service
metadata:
  name: {{ include "bnpl-service.fullname" . }}
  labels:
    {{- include "bnpl-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "bnpl-service.selectorLabels" . | nindent 4 }}
