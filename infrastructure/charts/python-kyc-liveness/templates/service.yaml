apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-kyc-liveness.fullname" . }}
  labels:
    {{- include "python-kyc-liveness.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "python-kyc-liveness.selectorLabels" . | nindent 4 }}
