apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-bvn-nin-verification.fullname" . }}
  labels:
    {{- include "go-bvn-nin-verification.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "go-bvn-nin-verification.selectorLabels" . | nindent 4 }}
