apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-fx-aggregator.fullname" . }}
  labels:
    {{- include "go-fx-aggregator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "go-fx-aggregator.selectorLabels" . | nindent 4 }}
