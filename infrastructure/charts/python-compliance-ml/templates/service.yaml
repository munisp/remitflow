apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-compliance-ml.fullname" . }}
  labels:
    {{- include "python-compliance-ml.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "python-compliance-ml.selectorLabels" . | nindent 4 }}
