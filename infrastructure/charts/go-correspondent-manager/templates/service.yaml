apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-correspondent-manager.fullname" . }}
  labels:
    {{- include "go-correspondent-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "go-correspondent-manager.selectorLabels" . | nindent 4 }}
