apiVersion: v1
kind: Secret
metadata:
  name: {{ include "go-security-hardening.fullname" . }}-secrets
type: Opaque
stringData:
{{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
