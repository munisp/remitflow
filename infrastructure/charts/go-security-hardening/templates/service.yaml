apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-security-hardening.fullname" . }}
  labels:
    {{- include "go-security-hardening.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "go-security-hardening.selectorLabels" . | nindent 4 }}
