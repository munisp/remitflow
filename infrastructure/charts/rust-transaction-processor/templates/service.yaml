apiVersion: v1
kind: Service
metadata:
  name: {{ include "rust-transaction-processor.fullname" . }}
  labels:
    {{- include "rust-transaction-processor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "rust-transaction-processor.selectorLabels" . | nindent 4 }}
