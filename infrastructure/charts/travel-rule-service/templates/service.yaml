apiVersion: v1
kind: Service
metadata:
  name: {{ include "travel-rule-service.fullname" . }}
  labels:
    {{- include "travel-rule-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "travel-rule-service.selectorLabels" . | nindent 4 }}
