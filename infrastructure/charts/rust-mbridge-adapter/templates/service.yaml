apiVersion: v1
kind: Service
metadata:
  name: {{ include "rust-mbridge-adapter.fullname" . }}
  labels:
    {{- include "rust-mbridge-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "rust-mbridge-adapter.selectorLabels" . | nindent 4 }}
