apiVersion: v1
kind: Service
metadata:
  name: {{ include "rust-device-fingerprint.fullname" . }}
  labels:
    {{- include "rust-device-fingerprint.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "rust-device-fingerprint.selectorLabels" . | nindent 4 }}
