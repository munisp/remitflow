apiVersion: v1
kind: Service
metadata:
  name: {{ include "rust-tigerbeetle-service.fullname" . }}
  labels:
    {{- include "rust-tigerbeetle-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "rust-tigerbeetle-service.selectorLabels" . | nindent 4 }}
