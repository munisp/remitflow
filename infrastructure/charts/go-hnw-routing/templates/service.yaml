apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-hnw-routing.fullname" . }}
  labels:
    {{- include "go-hnw-routing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "go-hnw-routing.selectorLabels" . | nindent 4 }}
