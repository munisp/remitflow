apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-papss-service.fullname" . }}
  labels:
    {{- include "go-papss-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "go-papss-service.selectorLabels" . | nindent 4 }}
