apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-bdc-connector.fullname" . }}
  labels:
    {{- include "go-bdc-connector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "go-bdc-connector.selectorLabels" . | nindent 4 }}
