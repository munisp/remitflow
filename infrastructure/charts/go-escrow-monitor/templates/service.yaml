apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-escrow-monitor.fullname" . }}
  labels:
    {{- include "go-escrow-monitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "go-escrow-monitor.selectorLabels" . | nindent 4 }}
