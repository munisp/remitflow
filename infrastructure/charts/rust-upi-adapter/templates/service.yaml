apiVersion: v1
kind: Service
metadata:
  name: {{ include "rust-upi-adapter.fullname" . }}
  labels:
    {{- include "rust-upi-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "rust-upi-adapter.selectorLabels" . | nindent 4 }}
