apiVersion: v1
kind: Secret
metadata:
  name: {{ include "rust-immigrant-worker-kyc.fullname" . }}-secrets
type: Opaque
stringData:
{{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
