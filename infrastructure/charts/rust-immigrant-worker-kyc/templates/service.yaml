apiVersion: v1
kind: Service
metadata:
  name: {{ include "rust-immigrant-worker-kyc.fullname" . }}
  labels:
    {{- include "rust-immigrant-worker-kyc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "rust-immigrant-worker-kyc.selectorLabels" . | nindent 4 }}
