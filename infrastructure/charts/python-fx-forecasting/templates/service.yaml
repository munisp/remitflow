apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-fx-forecasting.fullname" . }}
  labels:
    {{- include "python-fx-forecasting.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "python-fx-forecasting.selectorLabels" . | nindent 4 }}
