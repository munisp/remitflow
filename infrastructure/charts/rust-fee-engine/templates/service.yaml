apiVersion: v1
kind: Service
metadata:
  name: {{ include "rust-fee-engine.fullname" . }}
  labels:
    {{- include "rust-fee-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "rust-fee-engine.selectorLabels" . | nindent 4 }}
