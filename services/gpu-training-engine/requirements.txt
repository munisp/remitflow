# GPU-Agnostic Training Engine
# Core
torch>=2.0.0
numpy>=1.24.0
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
aiohttp>=3.9.0

# ONNX (cross-GPU inference)
onnx>=1.15.0
onnxruntime>=1.16.0

# Quantization
onnxruntime-extensions>=0.9.0

# Database integration
psycopg2-binary>=2.9.0

# Optional GPU-specific backends (install per hardware):
# NVIDIA:  pip install onnxruntime-gpu (CUDA EP + TensorRT EP)
# AMD:     pip install onnxruntime-rocm (ROCm EP)
# Intel:   pip install openvino onnxruntime-openvino intel-extension-for-pytorch
# Huawei:  pip install torch-npu (Ascend CANN EP)
# Apple:   pip install coremltools (CoreML EP) — macOS only
# DirectML (Windows): pip install onnxruntime-directml

# scikit-learn for preprocessing
scikit-learn>=1.3.0
