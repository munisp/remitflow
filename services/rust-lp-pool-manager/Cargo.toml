[package]
name = "rust-lp-pool-manager"
version = "0.1.0"
edition = "2021"
description = "RemitFlow LP Pool Manager — rebalancing, reserve tracking, collateral management"

[dependencies]
actix-web = "4"
actix-cors = "0.7"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
uuid = { version = "1", features = ["v4"] }
chrono = { version = "0.4", features = ["serde"] }
