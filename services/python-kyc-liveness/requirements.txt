fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.27.2
pydantic==2.10.3

# ── Open-source liveness detection (MiniFASNet, Apache 2.0) ──────────────────
uniface==2.0.0          # MiniFASNetV1SE + MiniFASNetV2 via ONNX Runtime
onnxruntime==1.20.1     # CPU-only ONNX inference engine

# ── Face matching ─────────────────────────────────────────────────────────────
deepface==0.0.93        # ArcFace / FaceNet embeddings (Apache 2.0)
opencv-python-headless==4.10.0.84

# ── Document OCR ─────────────────────────────────────────────────────────────
pytesseract==0.3.13     # Tesseract OCR Python wrapper
passporteye==2.2.1      # MRZ parser for travel documents
Pillow==11.0.0

# ── Utilities ────────────────────────────────────────────────────────────────
numpy==1.26.4
python-multipart==0.0.18

psycopg2-binary>=2.9
