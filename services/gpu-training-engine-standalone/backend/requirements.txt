# GPU Training Engine — Backend Dependencies
# Core
torch>=2.0.0
numpy>=1.24.0
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
aiohttp>=3.9.0

# Database
asyncpg>=0.29.0
psycopg2-binary>=2.9.0

# Caching
redis>=5.0.0

# ONNX (cross-GPU inference)
onnx>=1.15.0
onnxruntime>=1.16.0

# ML utilities
scikit-learn>=1.3.0

# Optional GPU-specific backends (install per hardware):
# NVIDIA:  pip install onnxruntime-gpu
# AMD:     pip install onnxruntime-rocm
# Intel:   pip install openvino onnxruntime-openvino intel-extension-for-pytorch
# Huawei:  pip install torch-npu
# Apple:   pip install coremltools
# Windows: pip install onnxruntime-directml
