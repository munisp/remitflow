apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: papss-integration-pdb
  namespace: remittance-platform
  labels:
    app: papss-integration
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: papss-integration
  unhealthyPodEvictionPolicy: AlwaysAllow

