# External Secrets for Remittance Platform
# These ExternalSecret resources pull secrets from AWS Secrets Manager or HashiCorp Vault
# and create Kubernetes Secrets that can be referenced by deployments

---
# Kafka SSL Secrets
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: kafka-ssl-secrets
  namespace: kafka-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: kafka-ssl-secrets
    creationPolicy: Owner
  data:
  - secretKey: ssl-keystore-password
    remoteRef:
      key: remittance/kafka/ssl
      property: keystore-password
  - secretKey: ssl-key-password
    remoteRef:
      key: remittance/kafka/ssl
      property: key-password
  - secretKey: ssl-truststore-password
    remoteRef:
      key: remittance/kafka/ssl
      property: truststore-password

---
# TigerBeetle Secrets
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: tigerbeetle-secrets
  namespace: tigerbeetle-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: tigerbeetle-secrets
    creationPolicy: Owner
  data:
  - secretKey: tls-cert
    remoteRef:
      key: remittance/tigerbeetle/tls
      property: cert
  - secretKey: tls-key
    remoteRef:
      key: remittance/tigerbeetle/tls
      property: key
  - secretKey: ca-cert
    remoteRef:
      key: remittance/tigerbeetle/tls
      property: ca-cert

---
# Permify Secrets
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: permify-secrets
  namespace: permify-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: permify-secrets
    creationPolicy: Owner
  data:
  - secretKey: api-key
    remoteRef:
      key: remittance/permify
      property: api-key
  - secretKey: postgres-user
    remoteRef:
      key: remittance/permify/database
      property: username
  - secretKey: postgres-password
    remoteRef:
      key: remittance/permify/database
      property: password

---
# Lakehouse Secrets
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: lakehouse-secrets
  namespace: lakehouse-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: lakehouse-secrets
    creationPolicy: Owner
  data:
  - secretKey: s3-access-key
    remoteRef:
      key: remittance/lakehouse/s3
      property: access-key
  - secretKey: s3-secret-key
    remoteRef:
      key: remittance/lakehouse/s3
      property: secret-key
  - secretKey: s3-endpoint
    remoteRef:
      key: remittance/lakehouse/s3
      property: endpoint
  - secretKey: postgres-password
    remoteRef:
      key: remittance/lakehouse/database
      property: password

---
# OpenAppSec Secrets
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: openappsec-secrets
  namespace: openappsec-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: openappsec-secrets
    creationPolicy: Owner
  data:
  - secretKey: agent-token
    remoteRef:
      key: remittance/openappsec
      property: agent-token
  - secretKey: fog-token
    remoteRef:
      key: remittance/openappsec
      property: fog-token

---
# Redis Secrets
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: redis-secrets
  namespace: redis-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: redis-secrets
    creationPolicy: Owner
  data:
  - secretKey: redis-password
    remoteRef:
      key: remittance/redis
      property: password

---
# Keycloak Secrets
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: keycloak-secrets
  namespace: keycloak-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: keycloak-secrets
    creationPolicy: Owner
  data:
  - secretKey: admin-password
    remoteRef:
      key: remittance/keycloak
      property: admin-password
  - secretKey: db-password
    remoteRef:
      key: remittance/keycloak/database
      property: password

---
# Temporal Secrets
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: temporal-secrets
  namespace: temporal-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: temporal-secrets
    creationPolicy: Owner
  data:
  - secretKey: db-password
    remoteRef:
      key: remittance/temporal/database
      property: password
  - secretKey: visibility-db-password
    remoteRef:
      key: remittance/temporal/visibility-database
      property: password

---
# PostgreSQL Secrets
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: postgresql-secrets
  namespace: postgresql-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: postgresql-secrets
    creationPolicy: Owner
  data:
  - secretKey: postgres-password
    remoteRef:
      key: remittance/postgresql
      property: postgres-password
  - secretKey: replication-password
    remoteRef:
      key: remittance/postgresql
      property: replication-password

---
# APISIX Secrets
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: apisix-secrets
  namespace: apisix-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: apisix-secrets
    creationPolicy: Owner
  data:
  - secretKey: admin-key
    remoteRef:
      key: remittance/apisix
      property: admin-key
  - secretKey: etcd-password
    remoteRef:
      key: remittance/apisix/etcd
      property: password

---
# Dapr Secrets
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: dapr-secrets
  namespace: dapr-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: dapr-secrets
    creationPolicy: Owner
  data:
  - secretKey: mtls-root-cert
    remoteRef:
      key: remittance/dapr/mtls
      property: root-cert
  - secretKey: mtls-issuer-cert
    remoteRef:
      key: remittance/dapr/mtls
      property: issuer-cert
  - secretKey: mtls-issuer-key
    remoteRef:
      key: remittance/dapr/mtls
      property: issuer-key

---
# Fluvio Secrets
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: fluvio-secrets
  namespace: fluvio-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: fluvio-secrets
    creationPolicy: Owner
  data:
  - secretKey: tls-cert
    remoteRef:
      key: remittance/fluvio/tls
      property: cert
  - secretKey: tls-key
    remoteRef:
      key: remittance/fluvio/tls
      property: key
